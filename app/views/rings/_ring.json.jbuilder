json.extract! ring, :id, :maximum_transmission_power, :baud_rate, :receiver_sensitivity, :effective_receiver_band, :codification, :link_distance, :created_at, :updated_at
json.url ring_url(ring, format: :json)
